{
  "name": "luxview",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "pnpm -r --parallel dev",
    "dev:web": "pnpm --filter @luxview/web dev",
    "dev:server": "pnpm --filter @luxview/server dev",
    "build": "pnpm -r build"
  }
}
